<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Users</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" 
    rel="stylesheet" 
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" 
    crossorigin="anonymous">
    <link rel="stylesheet" href="style.css">
  </head>
  <body class="body-bg">
    <br><br>
     <div class="text-center">
     <form method="GET" action="/user/new">
      <button class="btn rounded-pill text-bg-primary ps-4 pe-4 mt-3">Add New User</button>
     </form>
     <br><br> 
    </div>
    <h2 class="text-center text-primary fw-bold">List of all users</h2>
    <br><br>
    <div class="table-responsive">
      <table class="table table-hover table-dark table-bordered border-white align-middle">
        <thead class="table-dark">
          <tr class="justify-content-center">
            <th class="text-info" >Id</th>
            <th class="text-info">Email</th>
            <th class="text-info">Username</th>
            <th colspan="2" class="text-info">Options</th>
          </tr>
        </thead>
       <tbody>
        <% for(user of data) { %>
          <tr>
            <td class="text-info"><%= user.id %></td>
            <td class="text-info"><%= user.email %></td>
            <td class="text-info"><%= user.username %></td>
            <td>
              <form method="get" action="/user/<%=user.id%>/edit">
                <button class="btn badge rounded-pill text-bg-primary">Edit username</button>
              </form>
            </td>
            <td>
              <form method="get" action="/user/<%=user.id%>/delete">
                <button class="btn badge rounded-pill text-bg-danger">Delete</button>
              </form>
            </td>
          </tr>
          <% } %>
       </tbody>
      </table>  
    </div>
   </body>
</html>