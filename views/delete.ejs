<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Delete User</title>
  </head>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" 
    rel="stylesheet" 
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" 
    crossorigin="anonymous">
  <body class="bg-dark text-light">
  <div class="container mt-5 align-middle border border-warning-subtle">
    <div class="text-center p-3">
      <h3 class="mt-3">To delete this user : <p class="text-info"> <%= user.email %> </p>enter the password</h3>
     </div>
      <div class="mb-3 p-3 text-center container">
        <form method="post" action="/user/<%= user.id%>?_method=DELETE">
         <div class="mb-3">
          <input placeholder="enter password to delete" name="password" class="form-control"  />
         </div>
         <button type="delete" class="btn btn-outline-danger ">Delete</button>
        </form>
      </div>
  </div>  
    <script>
      let btn = document.querySelector("button");
      btn.addEventListener('click',(event)=>{
        if (!confirm("do you want to delete this user?")){
          event.preventDefault();
        }
      }) ;
    </script>
  </body>
</html>